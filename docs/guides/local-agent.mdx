---
title: Agente en local (próximamente)
description: Placeholder para el flujo 2 — clonar, API keys y ejecución del chat.
---

Esta página reservará instrucciones para:

- Clonar el repositorio desde GitHub.
- Configurar variables de entorno (incluidas claves de LLM cuando exista el agente).
- Ejecutar `npm install` y `npm run dev` para levantar sitio, docs y API.

Contenido pendiente de definición en una fase posterior.
